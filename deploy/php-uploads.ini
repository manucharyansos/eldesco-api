; Install as /etc/php/8.3/fpm/conf.d/99-eldesco.ini and reload php8.3-fpm.
; PHP's defaults (2 MB) would reject normal photos before Laravel sees them.
upload_max_filesize = 12M
post_max_size = 13M
memory_limit = 256M
